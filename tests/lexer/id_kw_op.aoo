
module import export type trait enum if else for break continue match return public private self op void auto as val ref in dup

importexport module  _type trait1 identifier1 _ __222 'a: '_: '___220000:

+ ++ += - -- -= * *= / /= % %= < <= << <<= > >= >> >>= | || |= & && &= ^ ^= ~ ! !! = == != ?? ?: : :: => -> . .. ... ; , ( ) { } [ ]

'f'

"g" b"g" c"g" r"g" f"g"  //line comments

/*

comment

*/